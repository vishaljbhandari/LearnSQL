/*
	View     
	-> a virtual table that does not physically exist
	-> It is stored in Oracle data dictionary and do not store any data
	-> It can be executed when called
	-> A view is created by a query joining one or more tables

	The naming conventions for a view should have a "vw_" prefix, followed by the namespace, results. The syntax should be "vw_<Result>". (lower case)
*/
